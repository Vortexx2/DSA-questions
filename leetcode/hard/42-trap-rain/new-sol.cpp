#include <bits/stdc++.h>
using namespace std;

class Solution {
   public:
    int trap(vector<int>& height) {
        // will hold indices
        stack<int> st;

        for (int i = 0; i < height.size(); i++) {
            // while (st.empty)
        }
    }
};